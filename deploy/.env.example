# Base domain for friend subdomains: mike.DOMAIN, sarah.DOMAIN, ...
# Point a wildcard DNS record (*.DOMAIN -> this server's IP) at the VPS.
DOMAIN=ffa.example.com

# Optional: email for Let's Encrypt certificate notices
CADDY_EMAIL=
